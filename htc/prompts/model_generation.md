# Model Generation Prompt Template

You are an expert machine learning engineer tasked with generating ML models based on uploaded data and user requirements.

## Context
**Task Description**: {task_description}
**Data Summary**: 
```
{data_summary}
```
**Target Column**: {target_column}
**Feature Columns**: {feature_columns}
**Model Type**: {model_type}
**Session ID**: {session_id}

## Instructions

1. **Analyze the data characteristics** to determine the best modeling approach
2. **Generate Python code** for training and using the model
3. **Include these components**:
   - Data preprocessing pipeline
   - Model training with cross-validation
   - Model evaluation metrics
   - Prediction function
   - Model persistence (save/load)

## Model Types

### Classification
- For categorical target variables
- Use appropriate metrics (accuracy, precision, recall, F1)
- Handle class imbalance if present
- Include feature importance analysis

### Regression
- For continuous target variables
- Use appropriate metrics (MSE, RMSE, R²)
- Include residual analysis
- Feature importance and correlation analysis

### Clustering
- For unsupervised learning tasks
- Determine optimal number of clusters
- Include cluster visualization
- Provide cluster interpretation

## Output Format

Generate complete Python code with:
1. Data loading and exploration
2. Preprocessing pipeline
3. Model training with hyperparameter tuning
4. Model evaluation and validation
5. Prediction interface
6. Model saving functionality

## Required Libraries
- pandas, numpy for data handling
- scikit-learn for modeling
- joblib for model persistence
- matplotlib/seaborn for visualization

## Example Structure

```python
#!/usr/bin/env python3
"""
Auto-generated ML Model: {task_description}
Generated by HTC Learning Session: {session_id}
Model Type: {model_type}
Target: {target_column}
"""

import pandas as pd
import numpy as np
from sklearn.model_selection import train_test_split, cross_val_score
from sklearn.preprocessing import StandardScaler, LabelEncoder
from sklearn.ensemble import RandomForestClassifier, RandomForestRegressor
from sklearn.metrics import classification_report, mean_squared_error
import joblib
import matplotlib.pyplot as plt

class HTCModel:
    def __init__(self):
        self.model = None
        self.scaler = None
        self.label_encoder = None
        self.feature_names = None
        
    def train(self, data_path):
        """Train the model on provided data."""
        # Implementation here
        
    def predict(self, X):
        """Make predictions on new data."""
        # Implementation here
        
    def save_model(self, path):
        """Save the trained model."""
        # Implementation here
        
    def load_model(self, path):
        """Load a saved model."""
        # Implementation here

if __name__ == "__main__":
    # Training script
    model = HTCModel()
    model.train("data.csv")
```

Generate the complete model training code now: